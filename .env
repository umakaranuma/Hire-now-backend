# Copy to .env and fill in values
# DJANGO_SETTINGS_MODULE=core.settings.development  # or core.settings.production

# Production
SECRET_KEY=your-secret-key
ALLOWED_HOSTS=yourdomain.com,api.yourdomain.com
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=hirenow
DB_USERNAME=root
DB_PASSWORD=umasiva1126@

# Optional: external services
PAYHERE_MERCHANT_ID=
# When using Firebase Phone Auth (Login & Register), OTP is sent by Firebase to the phone;
# the app uses Firebase client SDK (signInWithPhoneNumber). No backend SMS needed for that flow.
# Only if you use backend send-otp + SMS provider (Twilio etc.):
# SMS_API_KEY=

# Development: include OTP in send-otp response (Option 1 — 100% free). Show in console, enter manually. Do not use in production.
DEBUG_OTP_IN_RESPONSE=1

# Firebase Phone Auth – Firebase sends OTP to the phone; backend only verifies the ID token.
# Download JSON from Firebase Console → Project settings → Service accounts → Generate new private key
FIREBASE_ADMIN_SDK_PATH=firebase-adminsdk.json
